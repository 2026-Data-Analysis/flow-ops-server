spring:
  application:
    name: flowops
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  jackson:
    default-property-inclusion: non_null
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD:}
    algorithm: ${JASYPT_ENCRYPTOR_ALGORITHM:PBEWITHHMACSHA512ANDAES_256}
    key-obtention-iterations: ${JASYPT_ENCRYPTOR_KEY_OBTENTION_ITERATIONS:1000}
    salt-generator-classname: ${JASYPT_ENCRYPTOR_SALT_GENERATOR_CLASSNAME:org.jasypt.salt.RandomSaltGenerator}
    iv-generator-classname: ${JASYPT_ENCRYPTOR_IV_GENERATOR_CLASSNAME:org.jasypt.iv.RandomIvGenerator}
    string-output-type: ${JASYPT_ENCRYPTOR_STRING_OUTPUT_TYPE:base64}

logging:
  level:
    root: INFO
    flowops: INFO
    org.springframework.web: INFO

external:
  github:
    api-url: ${GITHUB_API_URL:https://api.github.com}
    token: ${GITHUB_TOKEN:}
    mock-enabled: ${GITHUB_MOCK_ENABLED:false}
    connect-timeout-millis: ${GITHUB_CONNECT_TIMEOUT_MILLIS:5000}
    read-timeout-millis: ${GITHUB_READ_TIMEOUT_MILLIS:20000}
    max-retries: ${GITHUB_MAX_RETRIES:2}
    retry-backoff-millis: ${GITHUB_RETRY_BACKOFF_MILLIS:500}
  ai:
    base-url: ${AI_SERVER_BASE_URL:http://localhost:8000}
    api-key: ${AI_SERVER_API_KEY:}
    connect-timeout-millis: ${AI_CONNECT_TIMEOUT_MILLIS:3000}
    read-timeout-millis: ${AI_READ_TIMEOUT_MILLIS:10000}

app:
  security:
    allowed-origins: "${APP_ALLOWED_ORIGINS:${APP_ALLOWED_ORIGIN:http://localhost:3000,http://localhost:5173,http://localhost:4173}}"
