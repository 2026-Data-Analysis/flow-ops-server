spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:flowops}
    username: ${DB_USERNAME:flowops}
    password: ${DB_PASSWORD:flowops}
    driver-class-name: org.postgresql.Driver

external:
  ai:
    mock-enabled: ${AI_MOCK_ENABLED:true}
